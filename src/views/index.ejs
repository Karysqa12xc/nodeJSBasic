<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node Js Basic</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
</head>

<body>

    <div class="topnav">
        <a href="/" class="active">Home</a>
        <a href="/upload">Upload file</a>
    </div>
    <div class="w3-container" style="padding-left: 16px;">
        <h2>List user:</h2>
        <table class="w3-table w3-striped w3-border">
            <thead>
                <th>ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Actions</th>
            </thead>
            <tbody>
                <% for(let i=0; i < dataUser.length; i++){ %>
                    <tr>
                        <td>
                            <%= dataUser[i].id %>
                        </td>
                        <td>
                            <%= dataUser[i].firstName %>
                        </td>
                        <td>
                            <%= dataUser[i].lastName %>
                        </td>
                        <td class="action-table">
                            <button type="button"><a href="/detail/user/<%=dataUser[i].id %>"
                                    target="_blank">Details</a></button>
                            <form action="/edit-user/<%=dataUser[i].id %>" method="get">
                                <button type="submit">Edit</button>
                            </form>
                            <form action="/delete-user" method="post">
                                <input type="text" hidden value="<%=dataUser[i].id %>" name="userDeleteId">
                                <button type="submit">Delete</button>
                            </form>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
    <div class="create-user-container">
        <div><b>Create a new user:</b></div>
        <form action="/create-new-user" method="POST">
            <div>
                <label>First Name: </label>
                <input type="text" name="FirstName" />
            </div>
            <div>
                <label>Last Name:</label>
                <input type="text" name="LastName" />
            </div>
            <div>
                <label>Email: </label>
                <input type="email" name="Email" />
            </div>
            <div>
                <label>Address: </label>
                <input type="text" name="address" />
            </div>
            <div>
                <button class="btn-submit" type="submit">Gửi</button>
            </div>
        </form>

    </div>

</body>

</html>